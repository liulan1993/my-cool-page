/** @type {import('next').NextConfig} */
const nextConfig = {
  // 如果您在旧的 next.config.ts 中有任何配置，请将它们复制到这里。
  // If you had any configurations in your old next.config.ts, copy them here.
};

module.exports = nextConfig;
